# Inference Settings for Comparison
project_name: "mnist-diffusion-comparison"

sampling:
  method: "ddim"
  steps: 50      # We jump 20 steps at a time (1000/50)
  eta: 0.0       # Deterministic
  batch_size: 8

model:
  base_channels: 64
